default:
  image: ruby:2.5

rspec:
  script: 
  - bundle exec rspec

rubocop:
  script: 
  - bundle exec rubocop

deploy:
  script:
  - gem install dpl
  - dpl --provider=heroku --app=gitlab-ci-ruby-test-staging --api-key=$HEROKU_STAGING_API_KEY